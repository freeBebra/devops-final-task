apiVersion: v1
kind: Service
metadata:
  name: shpagin-devops-final-task-service
spec:
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: shpagin-devops-final-task
  type: NodePort